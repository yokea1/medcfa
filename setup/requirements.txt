# MedCFA Python 依赖
#
# 安装：pip install -r requirements.txt
#
# 建议先建 conda 环境：
#   conda create -n medcfa python=3.10 -y
#   conda activate medcfa
#   pip install -r requirements.txt

# === 核心 ML 栈 ===
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.46.0
accelerate>=0.34.0
huggingface_hub>=0.25.0
hf_transfer>=0.1.8        # 加速 HF 下载
safetensors>=0.4.0
einops>=0.8.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# === 图像处理 ===
Pillow>=10.0.0
opencv-python>=4.8.0
scikit-image>=0.22.0      # SSIM 计算用
numpy>=1.26.0,<2.0.0

# === 数据 / 表格 ===
pandas>=2.1.0
pyarrow>=15.0.0           # parquet 读写
jsonlines>=4.0.0

# === medical 专用（sanity classifier 用）===
torchxrayvision>=1.2.0    # 预训练 CheXpert classifier，用于 mask sanity

# === Qwen-VL 系列推理 ===
qwen-vl-utils>=0.0.8      # Qwen2.5-VL / Qwen3-VL 官方推理工具

# === 实用工具 ===
tqdm>=4.66.0
loguru>=0.7.0
pyyaml>=6.0.1

# === Notebook / 调试（可选）===
ipython>=8.18.0
matplotlib>=3.8.0
seaborn>=0.13.0
